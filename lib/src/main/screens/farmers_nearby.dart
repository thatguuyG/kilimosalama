

import 'package:flutter/material.dart';

class NearbyFarmers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}